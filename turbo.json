{
    "$schema": "https://turbo.build/schema.json",
    "globalEnv": [
        "REDIS_URL"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                "dist/**",
                ".output/**",
                ".nuxt/**"
            ]
        },
        "test": {},
        "lint": {},
        "dev": {
            "cache": false,
            "persistent": true
        },
        "generate": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                "dist/**"
            ]
        }
    }
}